server:
  port: 80
spring:
  application:
    name: consul-consumer-payment-80
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}